
// Type ENUM

enum DesignColors {
    white = '#fff',
    black = '#000',
    red = '#f00',
    green = '#0f0',
    blue = '#00f'

}

// console.log(DesignColors.red);

enum StatusPermission {
    ADMIN = 1,
    USER = 2,
    SUPPORT = 0,
}

console.log(StatusPermission.SUPPORT);